language: generic 
sudo: required
services:
    - docker
before_install:
    - docker build -t kaitingpeck/docker-react -f Dockerfile.dev .
script:
    - docker run -e CI=true kaitingpeck/docker-react npm run test